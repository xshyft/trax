apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    version: "{{ .Chart.AppVersion }}"
spec:
  type: {{ .Values.traxcoord3.service.type }}
  ports:
    - port: {{ .Values.traxcoord3.service.port }}
      targetPort: {{ .Values.traxcoord3.service.targetPort }}
      protocol: TCP
      name: api
  selector:
    app: {{ .Chart.Name }}