apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    version: "{{ .Chart.AppVersion }}"
spec:
  type: {{ .Values.traxctrl.service.type }}
  ports:
    - port: {{ .Values.traxctrl.service.port }}
      targetPort: {{ .Values.traxctrl.service.targetPort }}
      protocol: TCP
      name: api
  selector:
    app: {{ .Chart.Name }}